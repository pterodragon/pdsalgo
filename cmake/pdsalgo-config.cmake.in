get_filename_component(pdsalgo_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
if(NOT TARGET pdsalgo::pdsalgo)
    include("${pdsalgo_CMAKE_DIR}/pdsalgo-targets.cmake")
endif()

set(pdsalgo_LIBRARIES pdsalgo::pdsalgo)
